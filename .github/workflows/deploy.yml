name: ♺ deploy 

on:
    workflow_call:
        inputs:
            env:
                description: Environment to deploy to
                required: true
                type: string

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running deployment on ${{ inputs.env }}..."
     